---
topic: Playground v2 — the full BMad runtime in the browser (all agents, all conversational workflows, stateful)
status: final
updated: 2026-07-15T19:29
---

- (decision) Working mode: Fast path (draft with [ASSUMPTION] tags, iterate on review)
- (decision) Code-executing dev workflows (quick-dev/dev-story/bmad-loop/code-review-exec): OUT ENTIRELY - permanent non-goal for Playground (no code sandbox, ever, for this product)
- (decision) Auth stays 2-person shared password; no user accounts in v2
- (decision) Sequencing: goals 2-4 FIRST on current architecture (fast visible wins for Vee), generic runtime after, migrate brainstorming onto runtime as parity proof. Rework risk accepted by user over runtime-first.
- (assumption) Skipped web-research subagents (personal-stakes foundation PRD); user may request landscape scan later
- (event) Fast-path PRD drafted: exec summary, goals+counter-metrics, UJ-1 (Mary->John chain), FR groups F1-F6 (FR-1..FR-43), NFR-1..6, phased scope A-D, risks, open questions. Addendum: adapter map, runtime-shaped schema, effort table, rejected alternatives, provider notes.
- (assumption) Tagged assumptions: print-CSS PDF; outbox send = builder-visible flag (no email); QStash only past function ceiling; web-search API (Tavily-ish) for research skills; function-calling-capable models required; agent picker ships phase D (Mary-only until then)
- (change) FR-40/41/43 reworked to a two-level agent->command TREE: level 1 pick agent (Mary default, all selectable), level 2 pick that agent's custom commands (full menu). Agents do far more than one skill (Mary = BP/MR/DR/TR/CB/WB/DP). Catalog organized by agent, parity-checked agent-by-agent.
- (decision) Web search resolved: FREE tier only (keyless/free default, builder-swappable), never paid. Other assumptions (print-CSS PDF, outbox=flag, QStash-past-ceiling, function-calling models) accepted by user.
- (event) PRD finalized (light finalize: personal-stakes foundation PRD; reviewer ceremony skipped per stakes). User authorized proceeding to subsequent docs + development.
